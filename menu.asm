; B0 - light shading
; B1 - Med shading
; B2 - Heavy shading
; B3 - single column
; B4 - single column splitter (L)
; B5 - single column /w/ double column splitter (L)
; B6 - double column /w/ single column splitter (L)
; B7 - single column H, double column V, upper-right corner
; B8 - double column H, single column V, upper-right corner
; B9 - double column splitter (L)
; BA - double column V
; BB - double column upper-right corner
; BC - double column lower-right corner
; BD - single column H, double column V, lower-right corner
; BE - double column H, single column V, lower-right corner
; BF - single column upper-right corner
; C0 - single column lower-left corner
; C1 - single column splitter (H, up)
; C2 - single column splitter (H, down)
; C3 - single column splitter (R)
; C4 - single column H
; C5 - single column 4-way
; C6 - double column H, single column V splitter (R)
; C7 - single column H, double column V splitter (R)
; C8 - double column lower-left corner
; C9 - double column upper-left corner
; CA - double column splitter (H, up)
; CB - double column splitter (H, down)
; CC - double column splitter (R)
; CD - double column H
; CE - double column 4-way
; CF - double column H, single column V splitter (H, up)
; D0 - single column H, double column V splitter (H, up)
; D1 - double column H, single column V splitter (H, down)
; D2 - single column H, double column V splitter (H, down)
; D3 - single column H, double column V lower-left corner
; D4 - double column H, single column V lower-left corner
; D5 - double column H, single column V upper-right corner
; D6 - single column H, double column V upper-right corner
; D7 - single column H, double column V 4-way
; D8 - double column H, single column V 4-way
; D9 - single column lower-right corner
; DA - single column upper-left corner
; DB - full solid
; DC - half solid
; DD - skinny solid V
; DE - fatter solid V
; DF - fatter solid H
